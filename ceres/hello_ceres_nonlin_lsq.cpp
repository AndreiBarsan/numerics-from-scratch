/*
 * Solve a simple nonlinear least-squares problem using the Google Ceres solver.
 *
 *  (ParameterBlock) -> (CostFunction) -> (ResidualBlock)
 */

#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}